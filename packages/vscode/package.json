{
  "name": "spek-vscode",
  "displayName": "spek — OpenSpec Viewer",
  "description": "Browse OpenSpec specs and changes directly in VS Code",
  "version": "1.8.3",
  "publisher": "kewang",
  "license": "MIT",
  "icon": "icon.png",
  "homepage": "https://github.com/spekhq/spek#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/spekhq/spek.git",
    "directory": "packages/vscode"
  },
  "bugs": {
    "url": "https://github.com/spekhq/spek/issues"
  },
  "keywords": [
    "openspec",
    "spec",
    "sdd",
    "spec-driven-development",
    "ai"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "workspaceContains:openspec/config.yaml",
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "spek",
      "properties": {
        "spek.aggregateJjWorkspaces": {
          "type": "boolean",
          "default": false,
          "tags": ["experimental"],
          "markdownDescription": "**EXPERIMENTAL:** Enable experimental jj VCS workspace aggregation.\n\nThis will show specifications found in related jj workspaces and mirrors the git workspaces implementation. Off by default; has no effect when `jj` is not installed or the repository is not a jj repo."
        }
      }
    },
    "commands": [
      {
        "command": "spek.open",
        "title": "Open spek",
        "category": "spek"
      },
      {
        "command": "spek.search",
        "title": "Search OpenSpec",
        "category": "spek"
      },
      {
        "command": "spek.openDashboard",
        "title": "Open Dashboard",
        "category": "spek",
        "icon": "$(link-external)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "spek-sidebar",
          "title": "spek",
          "icon": "media/sidebar-icon.svg"
        }
      ]
    },
    "views": {
      "spek-sidebar": [
        {
          "id": "spek.specsView",
          "name": "Specs",
          "when": "spek.hasOpenSpec"
        },
        {
          "id": "spek.changesView",
          "name": "Changes",
          "when": "spek.hasOpenSpec"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "spek.openDashboard",
          "when": "view == spek.specsView",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "esbuild src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node --sourcemap",
    "watch": "npm run build -- --watch",
    "package": "vsce package --no-dependencies",
    "publish": "vsce publish --no-dependencies"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.24.0",
    "@vscode/vsce": "^3.0.0",
    "@spekjs/core": "*",
    "chokidar": "^5.0.0",
    "fuse.js": "^7.0.0"
  }
}
